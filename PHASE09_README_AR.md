# الحامي PDF — 0.9.2

إدارة صفحات النماذج التفاعلية مع الاحتفاظ بالحقول وقيمها ومظهرها.

## التثبيت والتجربة

أغلق الحامي PDF وVisual Studio. فك الحزمة في مجلد منفصل عن مشروعك.
افتح PowerShell بجانب install.ps1 وشغّل:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1 -ProjectRoot "E:\C Sharp Projects\HamiPdf"
cd "E:\C Sharp Projects\HamiPdf"
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-run.ps1
```

بعد نجاح التجربة، أغلق البرنامج وأنشئ وثبّت الإصدار الجديد:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-installer.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\install-machine.ps1
```

مسار تثبيت البرنامج: C:\Program Files\HamiPdf. تحديث ملفات المشروع وحده لا يحدّث نسخة سطح المكتب.

## الجديد

- إضافة نماذج AcroForm إلى إدارة الصفحات؛ ترتيب صفحاتها وتدويرها وحذف بعضها واستخراج المحدد ودمج الملفات.
- نقل شجرة الحقول مع الصفحات، مع الحفاظ على الحقول المشتركة بين صفحات المصدر نفسه.
- عند دمج مصادر مختلفة، تُضاف بادئات مثل Hami1_ وHami2_ لأسماء الحقول كي تبقى قيم النماذج مستقلة. لا يتغير النص المطبوع بجانب الحقول.
- مقارنة الحقول وقيمها وخياراتها ومستطيلاتها وبيانات مظهرها قبل الحفظ وبعده. يُوقف الحفظ إذا لم تتطابق.
- الحفظ في ملف جديد فقط؛ الملفات الأصلية لا تتغير.

## تجربة عملية

1. افتح 55.pdf واضغط «الصفحات». أضف نسخة أخرى من الملف.
2. دوّر صفحة وبدّل ترتيب الصفحتين ثم «حفظ الكل» باسم جديد.
3. أغلق إدارة الصفحات لعرض النسخة الناتجة، ثم افتح «تعبئة النماذج».
4. اكتب قيمتين مختلفتين في الحقل نفسه من الصفحتين، واحفظ نسخة وأعد فتحها.
5. جرّب استخراج صفحة واحدة، ثم تأكد من بقاء حقولها قابلة للتعبئة.

التعبئة نفسها تتم من «تعبئة النماذج»، لا من صورة المعاينة في إدارة الصفحات.

## حدود الدعم

- نماذج XFA وحقول التوقيع الرقمي والمستندات ذات التوقيع المعتمد لا تدخل مسار إدارة الصفحات.
- لا يشغّل الحامي سكربتات PDF أو الحسابات الآلية للنماذج. عند الدمج تتغير أسماء الحقول؛ المراجع البرمجية إلى الأسماء القديمة ليست مضمونة في برامج أخرى. هذا الإصدار يحافظ على قيم الحقول ومظهرها وقابلية تعبئتها، ولا يعتمد عليه للحفاظ على منطق حسابات أو سكربتات نموذج متقدم.
- المستند ذو شجرة حقول تالفة أو أسماء متعارضة داخله يُرفض برسالة، ولا تُحذف حقوله لتجاوز المشكلة.
- مجموع مصادر عملية النماذج بحد أقصى 256 ميغابايت؛ يمكن تقسيم العمليات الكبيرة.
- يلزم WebView2 كما في الإصدارات السابقة.

## نتائج التحقق

اجتاز بناء WPF من Linux باستخدام Windows targeting (بقي تحذير TextInput السابق).
اجتازت اختبارات محرّك Chromium دمج 55.pdf إلى 140 حقلًا، وفصل القيم، وإعادة التعبئة بالعربية، والحفظ وإعادة القراءة.
اجتازت نماذج اصطناعية اختبارات الدمج والترتيب والتدوير والاستخراج، والحقول المشتركة، وخيارات الاختيار والقوائم، وحماية الملفات من الاستبدال أو تغيّر القيم.
تطابقت صورة صفحة 55.pdf الأصلية مع صورتها المنقولة عند العرض باستخدام Poppler.
تشغيل نافذة WPF وربط WebView2 وإنشاء ملف Setup يحتاج Windows؛ لم يُنفّذ هنا.
