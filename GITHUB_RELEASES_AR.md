# الحامي PDF 0.11.0 — GitHub والتحديثات

تشمل الحزمة المسح الضوئي 0.10.0 الذي أكد المستخدم نجاح تجربته، وإضافة فحص التحديثات.

## 1. تحديث المشروع والتجربة

أغلق البرنامج وVisual Studio، وفك الحزمة في مجلد منفصل. نفّذ بجانب install.ps1:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1 -ProjectRoot "E:\C Sharp Projects\HamiPdf"
cd "E:\C Sharp Projects\HamiPdf"
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-run.ps1
```

الأمر ينظف ويختبر ويبني ثم يشغّل. افتح «حول التطبيق»: يظهر زر فحص التحديثات وخيار الفحص اليومي. قبل أول نشر، الرسالة اليدوية بأن مستودع النشر غير متاح متوقعة؛ لا يدّعي البرنامج أنك على أحدث إصدار عند فشل الاتصال. جرّب السكانر وإغلاق البرنامج كذلك.

## 2. رفع المصدر إلى GitHub

من مجلد المشروع:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\upload-github.ps1
```

يتحقق السكربت من حساب AwadhObaid ويستنسخ مستودع المصدر في مجلد مؤقت ويرفع فقط ملفات الحزمة المحددة. يحافظ على التاريخ ولا يستخدم force push. يتوقف إذا تغير الفرع البعيد عن الإصدار الذي جهزت عليه الحزمة. لا يرفع ملفات البناء أو السجلات أو source-upload.json. الناتج المطلوب `[OK] Uploaded HamiPdf 0.11.0`.

## 3. إنشاء مستودع المثبّتات والنشر

أغلق البرنامج، ثم نفّذ:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\publish-release.ps1
```

السكربت:
- يتأكد من أن المصدر المطابق رُفع إلى AwadhObaid/HamiPdf.
- يشغّل اختبارات المشروع وبناء المثبّت والتحقق من الواجهة المنشورة. يحتاج .NET 8 SDK وInno Setup 6.3+ وGit وGitHub CLI على Windows.
- ينشئ مستودع AwadhObaid/HamiPdf-Releases عامًا إن لم يوجد.
- يرفع إصدارًا مسودة مع المثبّت باسمه المتضمن للإصدار ونسخة باسم ثابت وSHA256SUMS.txt.
- ينزّل النسخ المرفوعة ويقارن بصمتها، ثم ينشر المسودة ويجعلها أحدث إصدار.
- لا يستبدل إصدارًا سبق نشره. إذا انقطع الرفع قبل النشر، أعد الأمر لاستكمال المسودة.

لا تعتبر النشر مكتملًا إلا عند ظهور `[OK] Published HamiPdf 0.11.0`.
بعد نجاح النشر سيكون رابط التحميل المباشر الثابت:
https://github.com/AwadhObaid/HamiPdf-Releases/releases/latest/download/HamiPdf-Setup.exe

المصدر: https://github.com/AwadhObaid/HamiPdf
الإصدارات: https://github.com/AwadhObaid/HamiPdf-Releases/releases

## 4. تحديث نسخة سطح المكتب

سكربت النشر يبني المثبّت لكنه لا يثبّته. بعد نجاحه:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install-machine.ps1
```

## كيف تصل التحديثات لاحقًا؟

يجب تثبيت 0.11.0 مرة واحدة؛ النسخ الأقدم لا تحتوي فحص التحديثات. بعد ذلك يقرأ البرنامج أحدث GitHub Release مستقر عند التشغيل، بحد أقصى مرة كل 24 ساعة لكل مستخدم. يعرض نافذة إذا كان رقم الإصدار أكبر من المثبت ويحتوي المثبّت المناسب. يمكن إجراء فحص يدوي في أي وقت أو تعطيل الفحص التلقائي.

«تنزيل التحديث» يفتح المثبّت الرسمي في المتصفح، ثم يغلق المستخدم البرنامج ويشغّل المثبّت. لا توجد عملية تثبيت صامتة ولا تنزيل في الخلفية. «لاحقًا» يؤجل التنبيه، و«تجاهل هذا الإصدار» يخفي التنبيه التلقائي لذلك الإصدار فقط؛ الفحص اليدوي يعرضه. انقطاع الإنترنت لا يمنع عمل PDF أو المسح. لا تُرفع المستندات عند الفحص، ولا يحتاج المستخدم حساب GitHub.

لكل تحديث مستقبلي يجب زيادة رقم المشروع، بناء مثبّت مطابق ونشر إصدار مستقر جديد في HamiPdf-Releases بعلامة vX.Y.Z ومرفق HamiPdf-Setup-X.Y.Z-win-x64.exe، وكتابة المزايا في ملاحظات الإصدار. النسخة الثابتة HamiPdf-Setup.exe تحافظ على رابط المشاركة. حزم التحديث اللاحقة تُجهّز بقائمة تحقق المصدر الجديدة؛ لا تعدّل أرقام الحزمة الحالية وحدها ثم تحاول نشرها، لأن فحص المطابقة سيمنع اختلاف المصدر عن النسخة المرفوعة.

## التحقق الذي أُجري هنا

نجح بناء WPF ونشر win-x64 واختبارات قراءة الإصدار ومقارنته ورفض الروابط غير المطابقة والمسودات والإصدارات التجريبية واختبارات PDF الخاصة بالمسح. تشغيل نافذة التحديث في Windows وInno Setup ورفع GitHub الفعلي يتطلب تشغيل الأوامر عندك. محاولة الكتابة من الاتصال هنا رُفضت بخطأ 403؛ لم يُرفع المصدر ولم يُنشأ مستودع الإصدارات من هنا.
