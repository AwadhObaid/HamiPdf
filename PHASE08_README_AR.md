# الحامي PDF — المرحلة 08 — 0.8.0

## الاستخدام

1. افتح ملف PDF في النافذة الرئيسية.
2. اضغط «تعبئة النماذج». النافذة تبدأ من آخر نسخة محفوظة على القرص؛ التعديلات التي أجريتها في عارض PDF الرئيسي ولم تحفظها لا تنتقل إليها.
3. اكتب داخل الحقول، وحدد خانات الاختيار. استخدم Tab للتنقل.
4. اضغط «حفظ نسخة» أو Ctrl+S وحدد اسم PDF جديدًا.
5. أغلق نافذة التعبئة؛ تفتح النسخة المحفوظة في العارض الرئيسي للطباعة.
6. لتعديلها لاحقًا، افتحها واضغط «تعبئة النماذج» مجددًا.

الحفظ يحافظ على شجرة حقول النموذج وقيمها. معالجة مظهر النص العربي لا تُسطّح الصفحة ولا تحذف الحقول؛ يُحفظ مظهر الحقل فقط كصورة واضحة داخل الحقل، وتظل قيمته النصية قابلة للتعديل. قد يختلف خط المظهر البديل عن خط النموذج الأصلي (Segoe UI). لا يُستبدل أصل جلسة التعبئة. عند استبدال نسخة أخرى موجودة تُحفظ نسخة سابقة بامتداد .bak.

## التثبيت والتنظيف والتشغيل

أغلق الحامي PDF وVisual Studio، وفك الحزمة في مجلد منفصل. من جوار install.ps1:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1 -ProjectRoot "E:\C Sharp Projects\HamiPdf"
cd "E:\C Sharp Projects\HamiPdf"
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-run.ps1
```

السكربت ينظف المشروع ويشغّل الفحوص السابقة وفحص النماذج الجديد ثم يبني ويشغّل البرنامج.

بعد نجاح التجربة، أغلق البرنامج وابن المثبّت:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-installer.ps1
```

شغّل HamiPdf-Setup-0.8.0-win-x64.exe الناتج لتحديث نسخة سطح المكتب.

## نطاق هذه المرحلة

- تعبئة حقول AcroForm، خصوصًا النصوص وخانات الاختيار، وحفظ نسخة تفاعلية.
- تكبير وتنقل بواجهة مختصرة وتنبيه عند إغلاق تعديلات غير محفوظة.
- تحقق قبل الحفظ من أسماء الحقول وعددها وقيمها، ثم تحقق إضافي بعد تجهيز مظهر العربية.
- ترتيب ودمج واستخراج صفحات النماذج ما زال غير مدعوم؛ استخدم زر التعبئة بدل إدارة الصفحات لهذه الملفات.
- نماذج XFA والتوقيعات الرقمية غير مدعومة. لا تنفَّذ حسابات JavaScript أو أزرار إرسال النموذج.
- حد الملف 64 ميغابايت. الملفات المحمية قد تتطلب فتحها وحفظها بأداة تدعم حمايتها.
- البرنامج ومحرك التعبئة يعملان محليًا، دون رفع مستندات أو جلب محرك العرض من الإنترنت.

## التجربة المطلوبة على Windows

افتح 55.pdf واكتب نصًا عربيًا ورقمًا وحدد خانة اختيار. احفظ نسخة، وأغلق نافذة التعبئة وافتح النسخة. تحقق من ظهور النص دون الحاجة إلى النقر داخل الحقل، ومن معاينة الطباعة. أعد فتح التعبئة وعدّل النص وألغِ الاختيار واحفظ باسم جديد. تحقق من بقاء الحقول قابلة للتعبئة. جرّب الإغلاق قبل الحفظ واختر «لا» للتأكد من بقاء عملك.

اختبارات FormChecks تعمل تلقائيًا على Windows: القيم العربية، ارتباط الحقول بعناصر الصفحة، وجود مظهر محفوظ، الحفظ المتكرر والنسخة الاحتياطية. فحص المتصفح الآلي اختبر 55.pdf خارج WPF؛ لا يغني عن تجربة محرك WebView2 والطباعة على Windows.
