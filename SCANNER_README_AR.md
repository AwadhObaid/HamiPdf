# الحامي PDF — الإصدار 0.10.0: المسح الضوئي

أغلق الحامي PDF وVisual Studio. فك الحزمة في مجلد منفصل ثم افتح PowerShell بجانب install.ps1:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1 -ProjectRoot "E:\C Sharp Projects\HamiPdf"
cd "E:\C Sharp Projects\HamiPdf"
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-run.ps1
```

الأمر الأخير ينظّف ويبني المشروع ويجري اختبارات الحفظ ثم يشغّله. يحتاج أول بناء إلى الإنترنت لاستعادة حزم NuGet الجديدة. لم تُرفع تغييرات إلى GitHub.

## الاستخدام

1. اضغط «مسح ضوئي» في الشريط الرئيسي.
2. اختر طريقة الاتصال: WIA أو TWAIN ‏32 بت أو TWAIN ‏64 بت أو TWAIN القديم أو الشبكة eSCL/AirScan.
3. تُبحث الأجهزة بالطريقة المختارة. إذا لم يظهر الجهاز، جرّب طريقة أخرى وثبّت تعريف الشركة المناسب. قد يظهر الجهاز نفسه في أكثر من طريقة.
4. اختر الجهاز ومصدر الورق والألوان والدقة وحجم الصفحة. الخيارات المتاحة في النافذة هي طلبات للماسح وليست إعلانًا عن قدراته؛ قد يرفض الجهاز إعدادًا لا يدعمه. يمكن استخدام نافذة تعريف الشركة في WIA/TWAIN، وتكون إعداداتها هي المعتمدة. eSCL يعتمد على اكتشاف الأجهزة في الشبكة المحلية.
5. اضغط «بدء المسح / إضافة أوراق». يمكنك تكرار العملية لإضافة أوراق من السطح الزجاجي أو استخدام المغذّي والوجهين إذا كان الجهاز يدعمهما.
6. اختر صفحة لمعاينتها؛ استخدم أعلى/أسفل والتدوير والحذف قبل الحفظ.
7. «حفظ PDF وفتحه» ينشئ ملفًا جديدًا. لا يستبدل الملف الموجود حتى عند تأكيد نافذة Windows الاستبدال؛ اختر اسمًا جديدًا.
8. «حفظ ثم إضافة للمستند المفتوح» يحفظ المسح أولًا في ملف مستقل، ثم يفتح إدارة الصفحات بالمستند الحالي متبوعًا بالمسح. راجع الترتيب واحفظ النسخة المدمجة باسم جديد. الملف الأصلي لا يتغير. دعم النماذج وحدودها هي نفسها في 0.9.2.

عند الإيقاف أو انقطاع الماسح أو انحشار الورق، تبقى الصفحات المكتملة في الجلسة ويمكن حفظها. إذا ظهرت نافذة تعريف الماسح، قد يلزم ضغط «إلغاء» فيها. تُمنع مغادرة النافذة أثناء عمل الجهاز أو الحفظ، ثم يُطلب تأكيد تجاهل الصفحات غير المحفوظة. تذكّر الجهاز والإعدادات محلي لكل مستخدم. الصور مؤقتة في جلسة محلية وتُحذف عند إغلاقها؛ ليست ملفات استرداد بعد انهيار النظام.

## التوافق والحدود

دعم واسع للأجهزة التي توفر WIA/TWAIN ولماسحات eSCL المكتشفة على الشبكة؛ لا ضمان لجميع الموديلات. بعض ماسحات الشبكة تحتاج تعريف الشركة أو إعدادات شبكة تسمح بالاكتشاف. لا يتضمن هذا الإصدار إدخال عنوان شبكة يدويًا أو OCR. PDF الناتج صور للصفحات، لا حقول قابلة للتعبئة تلقائيًا.

مكوّن TWAIN ‏32 بت ومكتبتا DSM ‏32/64 بت مضمّنة في النشر. تعريف الجهاز نفسه ليس مضمّنًا. يُستخدم مسار محلي مستقل لـTWAIN ‏64 بت، وعامل 32 بت للتعريفات القديمة. لا يلزم تثبيت تطبيق NAPS2.

## تجربة Windows المطلوبة

- ظهور زر المسح في البرنامج وفي النسخة المثبّتة.
- WIA ثم TWAIN حسب التعريفات المتاحة؛ جرّب 32 بت حتى على Windows ‏64 بت.
- ورقة واحدة، ثم عدة أوراق، ثم المغذّي والوجهين إن توفرا.
- تبديل اللون/الرمادي/أبيض وأسود والدقة؛ نافذة تعريف الشركة.
- ترتيب الصفحات وتدويرها وحذف صفحة؛ حفظ الناتج وفتحه وطباعته.
- المسح إلى مستند مفتوح، وحفظ الدمج باسم جديد مع مراجعة حقول أي نموذج.
- إيقاف المسح/إلغاء نافذة التعريف ثم إعادة المسح. جرّب تجاهل النتائج وإغلاق البرنامج للتأكد من خروج طبيعي.
- فصل الجهاز أو نفاد الورق: تظهر رسالة وتبقى الصفحات المكتملة متاحة للحفظ.

## تحديث نسخة سطح المكتب بعد نجاح التجربة

أغلق البرنامج:

```powershell
cd "E:\C Sharp Projects\HamiPdf"
powershell -NoProfile -ExecutionPolicy Bypass -File .\build-installer.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\install-machine.ps1
```

يتحقق إنشاء المثبّت من ملفات المسح ومن زر المسح ونافذته في البرنامج المنشور. المسار النهائي يظهر بعد `[OK] Setup`، واسم الملف `HamiPdf-Setup-0.10.0-win-x64.exe`.

عند حدوث مشكلة أرسل سجل `logs\phase10_*.log`. أخطاء المسح تُسجّل أيضًا في `%LOCALAPPDATA%\HamiPdf\logs\scanner.log`؛ لا يحتوي هذا السجل على صور الصفحات.
